name: agent-review
on:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  review:
    uses: CruGlobal/agent-review/.github/workflows/review.yml@main
    with:
      mode: standard
    secrets:
      anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
